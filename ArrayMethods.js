const products = ["ram", "motherboard", "monitor", "cpu", "gpu"];
const peripherals = ["mouse", "mousepad", "keyboard", "headphones", "usb"];

// const allProducts = [products, peripherals];

//Concatenation of Array
// allProducts = products.concat(peripherals);

//Spread Operator(...)
// allProducts = [...products, ...peripherals];

//Creating array from variables
// allProducts = Array.of(products, peripherals).flat();

console.log(allProducts.sort());
